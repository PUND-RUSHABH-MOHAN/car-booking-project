const config = {
  cognito: {
    region: 'ap-south-1', // replace with your pool's region
    userPoolId: 'ap-south-1_LovOoIbly', // replace with your User Pool ID
    clientId: 'rg4bedstl03jhn2s3t1tgu46v', // replace with your App Client ID
  },
  invokeUrl: '' // Fill this in after Module 4
};

window.config = config;
